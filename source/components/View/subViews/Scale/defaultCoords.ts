import { PageCoords, ScaleCoords } from "Root/source/components/interfaces";

const defaultScaleCoords: ScaleCoords = { left: 1, bottom: 1, width: 1, height: 1 };
const defaultPageCoords: PageCoords = { pageX: 1, pageY: 1 };

export { defaultScaleCoords, defaultPageCoords }